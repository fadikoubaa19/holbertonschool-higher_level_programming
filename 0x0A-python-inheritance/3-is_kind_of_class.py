#!/usr/bin/python3
"""if it's the same return obj equal class"""


def is_kind_of_class(obj, a_class):
    """True if the object is an instance of, or if the object is an instance"""
    return isinstance(obj, a_class)
